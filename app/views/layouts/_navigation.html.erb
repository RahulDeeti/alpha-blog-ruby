<nav>
  <div>
    <div> 
      <ul>
          <li><%= link_to "Articles", articles_path %></li>
          <li><%= link_to "Users", users_path %></li>
          <li>
            <a href="#" >Actions <span></span></a>
              <% if logged_in? %>
              <ul>
                <li><%= link_to "New Article", new_article_path %></li>
              </ul>
             <% end %>
          </li>
      </ul>

      <% if logged_in? %>
        <li>
          <%= "(Admin)" if current_user.admin? %>
          <%= link_to current_user.username, user_path(current_user) %>
        </li>
          <li>
          <%= link_to 'Log out', logout_path %>
          </li>
          <li >
            <ul>
              <li><%= link_to "Edit your profile", edit_user_path(current_user) %></li>
              <li><%= link_to "View your profile", user_path(current_user) %></li>
              <li><%= button_to "Delete profile", user_path(current_user), method: :delete, data: { confirm: "Are you sure?" } %> </li>
            </ul>
          </li>
      <% else %>
          <li><%= link_to 'Log in', login_path %></li>
          <li><%= link_to 'Sign up', signup_path %></li>
      <% end %>
      <li>
      <a " href="#">
        Categories
      </a>
      </li> 
      <div>
        <li><% if logged_in? && current_user.admin? %>
          <%= link_to "Create new category", new_category_path, class: "dropdown-item" %>
        <% end %> </li> 
          <li><%= link_to "View categories", categories_path, class: "dropdown-item" %></li>
      </div>
   

    </div>
  </div>
</nav>